server:
  port: 9092
spring:
  application:
    name: GATEWAY-SERVER  
  boot:
    admin:
      client:
        url: http://localhost:1111/
eureka:
  client:
    register-with-eureka: false
    serviceUrl:
         defaultZone: http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'